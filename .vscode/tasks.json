{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Prettier",
            "type": "shell",
            "command": "npx prettier --write ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}